# 🚀 Деплой Thesauros Monitoring UI на Hetzner

Это пошаговое руководство поможет вам задеплоить приложение на Hetzner Cloud.

## 📋 Предварительные требования

1. **Аккаунт Hetzner Cloud**
   - Зарегистрируйтесь на [cloud.hetzner.com](https://cloud.hetzner.com)
   - Добавьте способ оплаты

2. **SSH ключ**
   - Создайте SSH ключ: `ssh-keygen -t rsa -b 4096`
   - Добавьте публичный ключ в Hetzner Cloud Console

3. **Домен (опционально)**
   - Если у вас есть домен, настройте A-запись на IP сервера

## 🎯 Варианты деплоя

### Вариант 1: Автоматический деплой (для продвинутых)

```bash
# Установка Hetzner CLI
curl -fsSL https://cli.hetzner.com/install.sh | bash

# Настройка токена
export HETZNER_TOKEN="ваш_токен"

# Запуск автоматического деплоя
chmod +x deploy-hetzner.sh
./deploy-hetzner.sh
```

### Вариант 2: Ручной деплой (рекомендуется для новичков)

```bash
chmod +x manual-deploy.sh
./manual-deploy.sh
```

## 📝 Пошаговая инструкция (ручной деплой)

### Шаг 1: Создание VPS

1. Зайдите в [Hetzner Cloud Console](https://console.hetzner.cloud/)
2. Нажмите **"Add Server"**
3. Заполните форму:
   - **Name**: `thesauros-monitoring`
   - **Location**: `Nuremberg (nbg1)`
   - **Image**: `Ubuntu 22.04`
   - **Type**: `CX11` (2 vCPU, 2GB RAM, ~€5/месяц)
   - **SSH Key**: выберите ваш ключ
4. Нажмите **"Create & Buy Now"**

### Шаг 2: Получение IP адреса

1. Скопируйте IP адрес сервера
2. Запустите скрипт деплоя:
   ```bash
   ./manual-deploy.sh
   ```
3. Введите IP адрес когда попросит

### Шаг 3: Настройка домена (опционально)

Если у вас есть домен:

1. В вашем DNS провайдере создайте A-запись:
   ```
   monitoring.yourdomain.com → IP_АДРЕС_СЕРВЕРА
   ```

2. Подождите 5-10 минут для распространения DNS

3. В скрипте введите ваш домен

## 🔧 Архитектура деплоя

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Пользователь  │───▶│     Nginx       │───▶│  Docker Container│
│                 │    │   (Port 80/443) │    │  (Port 3001)    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                              │
                              ▼
                       ┌─────────────────┐
                       │   SSL/HTTPS     │
                       │   (Certbot)     │
                       └─────────────────┘
```

## 📊 Что устанавливается на сервере

- **Docker** - для контейнеризации приложения
- **Docker Compose** - для управления контейнерами
- **Nginx** - веб-сервер и прокси
- **Certbot** - для SSL сертификатов
- **UFW** - файрвол
- **Systemd** - автозапуск сервиса

## 🌐 Доступ к приложению

После успешного деплоя:

- **HTTP**: `http://IP_АДРЕС_СЕРВЕРА`
- **HTTPS**: `https://ваш_домен.com` (если настроен домен)

## 🔍 Проверка работы

```bash
# Проверка статуса
curl http://IP_АДРЕС_СЕРВЕРА/api/health

# Подключение к серверу
ssh root@IP_АДРЕС_СЕРВЕРА

# Просмотр логов
docker-compose -f /opt/thesauros-monitoring/docker-compose.yml logs -f

# Перезапуск приложения
docker-compose -f /opt/thesauros-monitoring/docker-compose.yml restart
```

## 🛠️ Управление сервером

### Основные команды

```bash
# Подключение к серверу
ssh root@IP_АДРЕС_СЕРВЕРА

# Статус приложения
systemctl status thesauros-monitoring

# Перезапуск приложения
systemctl restart thesauros-monitoring

# Просмотр логов
journalctl -u thesauros-monitoring -f

# Обновление приложения
cd /opt/thesauros-monitoring
git pull
docker-compose down
docker-compose up -d --build
```

### Мониторинг ресурсов

```bash
# Использование CPU и памяти
htop

# Использование диска
df -h

# Использование сети
iftop

# Логи системы
tail -f /var/log/syslog
```

## 🔒 Безопасность

### Настроенный файрвол (UFW)
- Порт 22 (SSH) - открыт
- Порт 80 (HTTP) - открыт
- Порт 443 (HTTPS) - открыт
- Все остальные порты - закрыты

### SSL сертификаты
- Автоматическое получение через Let's Encrypt
- Автообновление каждые 90 дней

## 💰 Стоимость

- **CX11**: ~€5/месяц (2 vCPU, 2GB RAM, 20GB SSD)
- **CX21**: ~€10/месяц (3 vCPU, 4GB RAM, 40GB SSD) - для большей нагрузки

## 🚨 Устранение неполадок

### Приложение не запускается

```bash
# Проверка логов Docker
docker-compose -f /opt/thesauros-monitoring/docker-compose.yml logs

# Проверка статуса контейнеров
docker ps -a

# Пересборка контейнеров
docker-compose -f /opt/thesauros-monitoring/docker-compose.yml down
docker-compose -f /opt/thesauros-monitoring/docker-compose.yml up -d --build
```

### Проблемы с Nginx

```bash
# Проверка конфигурации
nginx -t

# Перезапуск Nginx
systemctl restart nginx

# Просмотр логов
tail -f /var/log/nginx/error.log
```

### Проблемы с SSL

```bash
# Проверка сертификатов
certbot certificates

# Обновление сертификатов
certbot renew

# Проверка автообновления
crontab -l
```

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что все порты открыты
3. Проверьте DNS настройки (если используете домен)
4. Обратитесь к документации Hetzner

## 🔄 Обновление приложения

```bash
# Подключение к серверу
ssh root@IP_АДРЕС_СЕРВЕРА

# Переход в директорию приложения
cd /opt/thesauros-monitoring

# Остановка приложения
docker-compose down

# Обновление кода (если используете git)
git pull

# Пересборка и запуск
docker-compose up -d --build

# Проверка статуса
docker-compose ps
```

---

**Удачи с деплоем! 🎉**
