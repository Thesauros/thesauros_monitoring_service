apiVersion: v1
kind: Secret
metadata:
  name: thesauros-secrets
type: Opaque
data:
  # Base64 encoded values - replace with your actual values
  arbitrum-rpc-url: <base64-encoded-rpc-url>
  # Example: echo -n "https://arb1.arbitrum.io/rpc" | base64
